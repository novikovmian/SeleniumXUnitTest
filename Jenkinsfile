pipeline {
    agent any
    stages {
        stage('build') {
            echo 'Building my application'
        }

        stage('test') {
            echo 'Testing my application'
        }

        stage('deploy') {
            echo 'Deploying my application'
        }
    }
}
